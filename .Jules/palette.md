## YYYY-MM-DD - [Clear Button in Search Inputs]
**Learning:** Adding an absolute positioned clear button (e.g., an `XIcon`) inside search inputs that conditionally renders when the query length is > 0 is a standard, preferred micro-UX pattern for this application's interface.
**Action:** Implement clear buttons for search inputs (`LocationSearch` and `MosqueFinder`) to enhance usability and make clearing searches more intuitive and faster for the user.
